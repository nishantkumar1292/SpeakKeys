package com.elishaazaria.sayboard.theme
import androidx.compose.ui.graphics.Color

// Primary palette - modern teal/blue accent
val Primary = Color(0xFF5CB8B2)         // Soft teal
val PrimaryDark = Color(0xFF4A9E99)     // Deeper teal
val PrimaryLight = Color(0xFF80CBC4)    // Light teal

// Surface colors
val DarkSurface = Color(0xFF1A1A2E)     // Deep navy-black
val DarkBackground = Color(0xFF16162A)  // Slightly darker
val DarkSurfaceVariant = Color(0xFF252542) // Cards / elevated surfaces

val LightSurface = Color(0xFFF5F5F5)
val LightBackground = Color(0xFFFFFFFF)
val LightSurfaceVariant = Color(0xFFE8E8E8)

// Status colors
val ListeningBlue = Color(0xFF4FC3F7)   // Active listening indicator
val ErrorRed = Color(0xFFEF5350)        // Error state
val ReadyGreen = Color(0xFF66BB6A)      // Ready state

// Legacy (kept for backward compat with user prefs)
val Green500 = Color(0xFF4CAF50)
val Green700 = Color(0xFF377A00)
val Orange700 = Color(0xFFFF9800)
val Orange900 = Color(0xFFE65100)
